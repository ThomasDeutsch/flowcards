import { ActionAndReactions } from "../../core/index.ts";

/**
 * a component that renders a list of actions and the corresponding reactions
 * @param actionsAndReactions - the recorded actions and reactions
 */
export default function ActionList()  {
    return <h1>hello</h1>
}