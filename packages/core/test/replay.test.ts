import * as bp from "../src/bid";
import { testScenarios} from "./testutils";
import { ActionType } from '../src/action';
import { flow } from '../src/flow';

test("an array of actions can be used as a replay", () => {
    expect(1).toBe(1)
});
